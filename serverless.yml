service: aws-node-http-api-project
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-west-2

functions:
  api:
    handler: src/index.handler
    events:
      - httpApi:
          path: /
          method: get
